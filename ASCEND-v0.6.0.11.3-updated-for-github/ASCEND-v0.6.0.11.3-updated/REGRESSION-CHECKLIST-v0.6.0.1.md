# ASCEND v0.6.0.1 — Focused Regression Checklist

1. `/api/health` reports version `0.6.0.1`, `externalRequestsConfigured: true`, and all v0.6.0.1 feature markers.
2. Command shows one compact Shareable Request Link card; System does not duplicate it.
3. Open the share link, wait longer than five minutes if desired, complete the form, and press Send. A fresh Turnstile check runs at that moment and success appears only after storage confirms receipt.
4. Quests → Requests from People → Refresh shows the encrypted test request. Refresh cannot remain loading indefinitely.
5. Review as Quest opens a preview but awards no XP until accepted. Dismiss removes only the request.
6. Existing account data, Guest data, XP, quests, milestones, Recovery File, feedback, and Delete Account remain unchanged.
