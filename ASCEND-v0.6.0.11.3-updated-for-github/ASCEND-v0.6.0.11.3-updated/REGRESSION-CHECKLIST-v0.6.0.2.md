# ASCEND v0.6.0.2 — Focused Regression Checklist

1. Open `/api/health` directly from the browser address bar; it returns JSON with version `0.6.0.2`.
2. Confirm the normal ASCEND production page opens and account data remains unchanged.
3. Send one encrypted External Request and confirm it appears in Quests → Requests from People.
4. No Supabase SQL or Vercel environment-variable setup is required for this patch.
